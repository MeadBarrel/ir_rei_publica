rp_scheme_gain_support = {
	can_be_picked = {
		always = no
	}

	finished_when = {
		custom_tooltip = {
			text = "rp_scheme_gain_support_tooltip"
		}
	}

	duration = 720

	skip_initial_abort = yes

	abort = {
		OR = {
			
		}
	}
}