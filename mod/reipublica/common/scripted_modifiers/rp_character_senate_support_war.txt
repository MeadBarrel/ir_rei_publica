rp_character_senate_support_war_independent = {
	modifier = {
		add = 1.5  # carthage senate hates Rome
		employer = {
			tag = CAR
			scope:target = { tag = ROM }
		}
	}

	# Military party specific
	modifier = {  # Likes the idea of war by default
		add = 2
		party = military_party
	}

	modifier = {  # Is ok with just one war
		add = -3
		party = military_party
		employer = { war = yes }
	}

	# Civic party specific

	modifier = {  # Target is tribal
		add = 2
		party = civic_party
		scope:target = { is_tribal = yes }
	}

	modifier = {  # Different culture group
		add = 1
		party = civic_party
		NOT = { has_same_culture_group_as = scope:target.current_ruler }
	}

	modifier = {  # Different culture
		add = 1
		party = civic_party
		NOT = { has_same_culture_as = scope:target.current_ruler }
	}

	modifier = {  # Same culture group
		add = -1
		party = civic_party
		has_same_culture_group_as = scope:target.current_ruler
	}

	modifier = {  # Same culture
		add = -1
		party = civic_party
		has_same_culture_as = scope:target.current_ruler		
	}

	# Religious party specific

	modifier = {  # Ok with war
		add = 1
		party = religious_party
	}

	modifier = {  # Different religion
		add = 2
		NOT = { has_same_religion_as = scope:target.current_ruler }
	}

	modifier = {  # Same religion
		add = -2
		has_same_religion_as = scope:target.current_ruler
	}

	# Mercantile party specific
	modifier = {
		add = -2
		party = mercantile_party
		employer = { imports_from = scope:target }
	}

	# Populist party specific
	modifier = {
		add = 100
		party = populist_party
	}
}